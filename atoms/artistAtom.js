import { atom } from 'recoil';

export const artistIdsState = atom({
    key: 'artistIdsState',
    default: [],
});

export const playlist1State = atom({
    key: 'playlist1State',
    default: [],
});

export const playlist2State = atom({
    key: 'playlist2State',
    default: [],
});

export const playlist3State = atom({
    key: 'playlist3State',
    default: [],
});

export const playlist4State = atom({
    key: 'playlist4State',
    default: [],
});